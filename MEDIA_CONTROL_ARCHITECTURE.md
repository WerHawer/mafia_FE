# ✅ Зміни в архітектурі Media Controls - Серверне управління

## 🎯 Що змінено

### ❌ БУЛО (Гібридний підхід):
```typescript
// Користувач контролював свої треки локально через LiveKit Client API
if (isMyStream) {
  await room.localParticipant.setCameraEnabled(!currentlyEnabled);
}

// А потім відправлявся запит на сервер
socket.emit(wsEvents.toggleUserCamera, { ... });
```

**Проблеми:**
- ❌ Дублювання логіки (клієнт + сервер)
- ❌ Можливі рассинхронізації
- ❌ Складніше тестування
- ❌ GM не може коректно керувати користувачами

---

### ✅ СТАЛО (Повне серверне управління):
```typescript
// Завжди тільки відправка запиту на сервер
sendMessage(wsEvents.toggleUserCamera, {
  userId: targetUserId,
  enabled: !currentlyEnabled,
});

// Сервер керує всіма через LiveKit Server API
// Всі клієнти отримують оновлення через userCameraStatusChanged
```

**Переваги:**
- ✅ Єдине джерело правди - сервер
- ✅ Немає рассинхронізацій
- ✅ GM і користувачі працюють однаково
- ✅ Легше тестувати і підтримувати
- ✅ Централізований контроль доступу

---

## 📝 Детальні зміни

### 1. **useMediaControls.ts**

#### Видалено:
```typescript
import { useRoomContext } from "@livekit/components-react";

const room = useRoomContext();

// В toggleCamera:
if (isMyStream) {
  await room?.localParticipant.setCameraEnabled(!currentlyEnabled);
}

// В toggleMicrophone:
if (isMyStream) {
  await room?.localParticipant.setMicrophoneEnabled(!currentlyEnabled);
}
```

#### Додано:
```typescript
// Тільки відправка запиту на сервер
sendMessage(wsEvents.toggleUserCamera, {
  userId: targetUserId,
  enabled: !currentlyEnabled,
});
```

#### Функції більше не async:
```typescript
// БУЛО:
const toggleCamera = useCallback(async () => { ... });

// СТАЛО:
const toggleCamera = useCallback(() => { ... });
```

---

## 🔄 Новий потік даних

### Сценарій 1: Користувач вимикає свою камеру

```
┌──────────┐                    ┌──────────┐                    ┌──────────┐
│  Client  │                    │  Server  │                    │ LiveKit  │
│  (User)  │                    │          │                    │  Server  │
└────┬─────┘                    └────┬─────┘                    └────┬─────┘
     │                               │                               │
     │ Клік на іконку камери         │                               │
     │────────────────────────────>  │                               │
     │ toggleUserCamera              │                               │
     │ { userId: "user123",          │                               │
     │   enabled: false }            │                               │
     │                               │                               │
     │                               │ mutePublishedTrack            │
     │                               │──────────────────────────────>│
     │                               │ (roomName, userId,            │
     │                               │  CAMERA, muted=true)          │
     │                               │                               │
     │                               │<──────────────────────────────│
     │                               │         ✅ OK                 │
     │                               │                               │
     │ userCameraStatusChanged       │                               │
     │<──────────────────────────────│                               │
     │ { userId: "user123",          │                               │
     │   enabled: false }            │ (broadcast to all)            │
     │                               │                               │
     │ UI оновлюється                │                               │
     │ Іконка червона                │                               │
     │                               │                               │
```

### Сценарій 2: GM вимикає камеру користувача

```
┌──────────┐                    ┌──────────┐                    ┌──────────┐
│  Client  │                    │  Server  │                    │ LiveKit  │
│   (GM)   │                    │          │                    │  Server  │
└────┬─────┘                    └────┬─────┘                    └────┬─────┘
     │                               │                               │
     │ Клік на іконку камери         │                               │
     │ на відео user123              │                               │
     │────────────────────────────>  │                               │
     │ toggleUserCamera              │                               │
     │ { userId: "user123",          │                               │
     │   enabled: false }            │                               │
     │                               │                               │
     │                               │ Перевірка: isGM = true ✅     │
     │                               │                               │
     │                               │ mutePublishedTrack            │
     │                               │──────────────────────────────>│
     │                               │ (roomName, "user123",         │
     │                               │  CAMERA, muted=true)          │
     │                               │                               │
     │                               │<──────────────────────────────│
     │                               │         ✅ OK                 │
     │                               │                               │
     │ userCameraStatusChanged       │                               │
     │<──────────────────────────────│                               │
     │ { userId: "user123",          │                               │
     │   enabled: false }            │ (broadcast to all)            │
     │                               │                               │
     │ Всі клієнти отримують         │                               │
     │ оновлення і бачать            │                               │
     │ червону іконку для user123    │                               │
     │                               │                               │
```

---

## 🔐 Серверна перевірка прав

```typescript
socket.on('toggleUserCamera', async (data: { userId: string; enabled: boolean }) => {
  const { userId, enabled } = data;
  const requesterId = socket.data.userId;
  const isGM = socket.data.isGM;
  const roomName = socket.data.roomName;
  
  // ✅ Перевірка прав доступу
  if (requesterId !== userId && !isGM) {
    socket.emit('error', { message: 'Access denied' });
    return;
  }
  
  // ✅ Централізоване управління через LiveKit Server API
  await livekitClient.mutePublishedTrack(
    roomName, 
    userId, 
    TrackSource.CAMERA,
    !enabled
  );
  
  // ✅ Broadcast всім клієнтам
  io.to(roomName).emit('userCameraStatusChanged', {
    userId,
    enabled,
  });
});
```

---

## 📋 API LiveKit Server SDK

### Встановлення:
```bash
npm install livekit-server-sdk
# або
yarn add livekit-server-sdk
```

### Основні методи:

```typescript
import { RoomServiceClient, TrackSource } from 'livekit-server-sdk';

const client = new RoomServiceClient(
  process.env.LIVEKIT_URL,
  process.env.LIVEKIT_API_KEY,
  process.env.LIVEKIT_API_SECRET
);

// Мутити/анмутити трек
await client.mutePublishedTrack(
  roomName: string,
  userId: string,
  trackSource: TrackSource.CAMERA | TrackSource.MICROPHONE,
  muted: boolean
);

// Отримати інформацію про учасника
const participant = await client.getParticipant(roomName, userId);

// Отримати список всіх учасників
const participants = await client.listParticipants(roomName);

// Видалити учасника з кімнати
await client.removeParticipant(roomName, userId);
```

---

## ✅ Переваги нової архітектури

### 1. **Єдине джерело правди**
- Сервер - єдине місце, де приймаються рішення
- Всі клієнти завжди синхронізовані

### 2. **Безпека**
- Неможливо обійти перевірку прав на клієнті
- Всі дії логуються на сервері

### 3. **Простота**
- Менше коду на клієнті
- Немає дублювання логіки
- Легше тестувати

### 4. **Надійність**
- Використовується `sendMessage` з чергою
- Гарантована доставка
- Повторні спроби при помилках

### 5. **Універсальність**
- Однакова логіка для користувача і GM
- Не потрібні особливі випадки в коді

---

## 🧪 Тестування

### Перевірка на клієнті:

```javascript
// В DevTools Console:
// 1. Відкрити вкладку Console
// 2. Клікнути на контрол
// 3. Перевірити лог:

useMediaControls: Toggle camera request sent to server {
  userId: "user123",
  enabled: false,
  isMyStream: true,    // або false для GM
  isIGM: false,        // або true для GM
  canControl: true
}
```

### Перевірка на сервері:

```javascript
// В логах сервера:
Camera disabled for user user123 by user123
// або
Camera disabled for user user123 by gm_user_456
```

### Перевірка LiveKit:

```bash
# Перевірити стан треків через LiveKit CLI
livekit-cli room list-participants --room ROOM_NAME

# Результат покаже muted треки:
Participant: user123
  - Camera: muted=true
  - Microphone: muted=false
```

---

## 📊 Порівняльна таблиця

| Аспект | Старий підхід | Новий підхід |
|--------|--------------|--------------|
| **Управління своїми медіа** | Client API + Server | ✅ Тільки Server |
| **Управління GM** | Не працювало | ✅ Працює через Server |
| **Синхронізація** | Можливі рассинхронізації | ✅ Завжди синхронізовано |
| **Безпека** | Частково на клієнті | ✅ Повністю на сервері |
| **Код на клієнті** | Складніше | ✅ Простіше |
| **Тестування** | Важче | ✅ Легше |
| **Логування** | Частково | ✅ Повне на сервері |

---

## 🚀 Готовність до production

### Frontend ✅
- [x] Видалено прямий контроль LiveKit Client API
- [x] Всі запити йдуть через `sendMessage`
- [x] Надійна доставка з чергою
- [x] Детальне логування
- [x] TypeScript компіляція успішна

### Backend (Потрібно реалізувати) 🔧
- [ ] Обробники `toggleUserCamera` та `toggleUserMicrophone`
- [ ] Інтеграція з LiveKit Server SDK
- [ ] Перевірка прав доступу
- [ ] Broadcast подій `userCameraStatusChanged` та `userMicrophoneStatusChanged`
- [ ] Логування всіх дій
- [ ] Обробка помилок

---

## 📚 Документація

- `MEDIA_CONTROL_SERVER_GUIDE.md` - Повна інструкція для серверної реалізації
- `MEDIA_CONTROL_FINAL_CHECK.md` - Детальна перевірка коду
- LiveKit Server SDK: https://docs.livekit.io/server-sdk-js/

---

**Готово до тестування з реалізованим backend!** 🎉

